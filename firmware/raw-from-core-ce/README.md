# Raw sketches from PSF Core

Holding area. These `.ino` files are imported verbatim from `core-ce/robot/` so we don't lose them. They are **not** templates yet — they're starting material.

To turn one into a Helm template, see `firmware/templates/TODO.md`.
